1. install docker https://docs.docker.com/get-docker/
---- make sure docker-compose is installed with the command docker-compose version
2. clone the grubberbot repo
3. open a command line prompt and navigate to the grubberbot directory
4. build the docker image
docker build --pull --no-cache --tag test-grubberbot .
docker build --tag test-grubberbot .

--still writing steps

docker-compose up --build --detach --force-recreate --remove-orphans && docker image prune --force --all

docker-compose up --build --force-recreate --remove-orphans
